pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "Running ${env.JOB_NAME} and the ${BUILD_NUMBER} on ${env.JENKINS_URL}"
            }
        }
    }
}
